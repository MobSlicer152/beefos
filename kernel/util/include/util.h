#pragma once

#include <stdint.h>

// Reverse a word
extern uint16_t reverse_word(uint16_t word);

// Reverse a doubleword
extern uint32_t reverse_dword(uint32_t dword);

// Reverse a quadword
extern uint64_t reverse_qword(uint64_t qword);
